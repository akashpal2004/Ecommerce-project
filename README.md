# Ecommerce-project
A simple e-commerce web application built using Django. It includes product listing, category management, and a user-friendly interface for browsing items. The project demonstrates core concepts like models, views, templates, and static files in Django, along with basic CRUD operations for managing products.
